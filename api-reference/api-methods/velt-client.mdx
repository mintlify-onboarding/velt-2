{/* <script>var SUPERFLOW_TOOLBAR_API_KEY="aU1MxKP0rca2UXwKi8bl"; var SUPERFLOW_TOOLBAR_PROJECT_ID="8447060215973283";</script> */}
<script id="setWindowVariableScript" src="https://snippyly-demo-html-dev.web.app/set-window-variable.js?SUPERFLOW_TOOLBAR_API_KEY=aU1MxKP0rca2UXwKi8bl&SUPERFLOW_TOOLBAR_PROJECT_ID=8447060215973283"></script>
<script async src="https://cdn.jsdelivr.net/npm/@usesuperflow/toolbar/superflow.min.js"></script>

<Steps>
<Step title="loadSnippyly()">
    Loads the Snippyly service, enabling its functionality in the application.
    ```jsx
    client.loadSnippyly();
    ```
</Step>
<Step title="loadVelt()">
    Loads the Velt service, allowing users to access Velt-related features.
    ```jsx
    client.loadVelt();
    ```
</Step>
<Step title="configService()">
    Configures the application's service settings and options.
    ```jsx
    client.configService();
    ```
</Step>
<Step title="contactService()">
    Manages contact-related operations, such as adding, removing, or updating contacts.
    ```jsx
    client.contactService();
    ```
</Step>
<Step title="docService()">
    Handles document-related operations, such as creating, editing, and saving documents.
    ```jsx
    client.docService();
    ```
</Step>
<Step title="authService()">
    Manages user authentication and authorization, allowing access to secured features.
    ```jsx
    client.authService();
    ```
</Step>
<Step title="presenceService()">
    Manages user presence and online status, providing information about who is currently active.
    ```jsx
    client.presenceService();
    ```
</Step>
<Step title="cursorService()">
    Handles cursor-related functions, such as showing the position of other users' cursors in a shared document.
    ```jsx
    client.cursorService();
    ```
</Step>
<Step title="domService()">
    Manages Document Object Model (DOM) operations, interacting with the structure of web pages or documents.
    ```jsx
    client.domService();
    ```
</Step>
<Step title="commentService()">
    Manages comments in documents, allowing users to add, edit, or remove comments on specific content.
    ```jsx
    client.commentService();
    ```
</Step>
<Step title="highlightService()">
    Manages highlights and annotations, making it possible to highlight and mark specific content in documents.
    ```jsx
    client.highlightService();
    ```
</Step>
<Step title="tagService()">
    Manages tags and labels, which can be used to categorize or organize content in documents.
    ```jsx
    client.tagService();
    ```
</Step>
<Step title="recorderAnnotationService()">
    Handles recorder annotations, possibly related to recording and annotating audio or video content.
    ```jsx
    client.recorderAnnotationService();
    ```
</Step>
<Step title="recorderService()">
    Manages recording services, which can include audio or video recording features.
    ```jsx
    client.recorderService();
    ```
</Step>
<Step title="loggingService()">
    Handles logging and activity tracking, recording user interactions and events for analysis or debugging.
    ```jsx
    client.loggingService();
    ```
</Step>
<Step title="rewriterService()">
    Manages document rewriting and editing, enabling changes to the content of documents.
    ```jsx
    client.rewriterService();
    ```
</Step>
<Step title="documentUserAccessService()">
    Manages user access to documents, controlling who can view, edit, or share specific documents.
    ```jsx
    client.documentUserAccessService();
    ```
</Step>
<Step title="themeService()">
    Manages document themes and styles, allowing users to customize the appearance of documents.
    ```jsx
    client.themeService();
    ```
</Step>
<Step title="huddleService()">
    Manages huddle (group) activities, facilitating collaborative activities with groups of users.
    ```jsx
    client.huddleService();
    ```
</Step>
<Step title="flockService()">
    Manages flock (group) activities, possibly related to group communication or collaboration.
    ```jsx
    client.flockService();
    ```
</Step>
<Step title="iamService()">
    Manages Identity and Access Management (IAM), controlling user access and permissions.
    ```jsx
    client.iamService();
    ```
</Step>
<Step title="translateService()">
    Handles translation services, allowing users to translate content from one language to another.
    ```jsx
    client.translateService();
    ```
</Step>
<Step title="liveStateService()">
    Manages live document state updates, keeping documents in sync across users in real time.
    ```jsx
    client.liveStateService();
    ```
</Step>
<Step title="initConfig()">
    Initializes the configuration settings for the application or service.
    ```jsx
    client.initConfig();
    ```
</Step>
<Step title="identify()">
    Identifies and authenticates users, ensuring that the user is who they claim to be.
    ```jsx
    client.identify();
    ```
</Step>
<Step title="updateUser()">
    Updates user information, such as user profiles, preferences, or settings.
    ```jsx
    client.updateUser();
    ```
</Step>
<Step title="signOutUser()">
    Signs out a user, logging them out of the application or service.
    ```jsx
    client.signOutUser();
    ```
</Step>
<Step title="setDocumentId()">
    Sets the document identifier, specifying a unique ID for a particular document.
    ```jsx
    client.setDocumentId();
    ```
</Step>
<Step title="getDocumentId()">
    Retrieves the document identifier for a specific document.
    ```jsx
    client.getDocumentId();
    ```
</Step>
<Step title="setDocumentParams()">
    Sets document parameters, potentially configuring document settings or attributes.
    ```jsx
    client.setDocumentParams();
    ```
</Step>
<Step title="setLocation()">
    Sets the user's location, possibly for location-based features or services.
    ```jsx
    client.setLocation();
    ```
</Step>
<Step title="addLocation()">
    Adds a new location to the user's profile, storing information about user locations.
    ```jsx
    client.addLocation();
    ```
</Step>
<Step title="setScrollableElementsIds()">
    Sets the IDs of scrollable elements, which can be used for managing scrollable areas in documents or applications.
    ```jsx
    client.setScrollableElementsIds();
    ```
</Step>
<Step title="removeDocumentParams()">
    Removes document parameters, reverting or changing document settings.
    ```jsx
    client.removeDocumentParams();
    ```
</Step>
<Step title="removeLocation()">
    Removes a location from the user's profile, typically used for location-based services.
    ```jsx
    client.removeLocation();
    ```
</Step>
<Step title="removeScrollableElementsIds()">
    Removes scrollable element IDs, potentially altering scrollable areas within documents or applications.
    ```jsx
    client.removeScrollableElementsIds();
    ```
</Step>
<Step title="getPresenceElement()">
    Retrieves the presence element, possibly to display user presence information in the user interface.
    ```jsx
    client.getPresenceElement();
    ```
</Step>
<Step title="getCursorElement()">
    Retrieves the cursor element, which may be used to display or manage user cursors in collaborative environments.
    ```jsx
    client.getCursorElement();
    ```
</Step>
<Step title="getCommentElement()">
    Retrieves the comment element, which could be used to display or manage comments within documents or content.
    ```jsx
    client.getCommentElement();
    ```
</Step>
<Step title="getTagElement()">
    Retrieves the tag element, possibly used for displaying or managing tags or labels in documents.
    ```jsx
    client.getTagElement();
    ```
</Step>
<Step title="getSelectionElement()">
    Retrieves the selection element, possibly used for managing and displaying selected content in documents.
    ```jsx
    client.getSelectionElement();
    ```
</Step>
<Step title="getRecorderElement()">
    Retrieves the recorder element, possibly used for displaying or managing recording features.
    ```jsx
    client.getRecorderElement();
    ```
</Step>
<Step title="getContactElement()">
    Retrieves the contact element, which may be used for displaying or managing contact information or user profiles.
    ```jsx
    client.getContactElement();
    ```
</Step>
<Step title="getRewriterElement()">
    Retrieves the rewriter element, which may be used for managing document rewriting and editing features.
    ```jsx
    client.getRewriterElement();
    ```
</Step>
<Step title="getHuddleElement()">
    Retrieves the huddle element, which may be used to manage and display huddle-related activities or content.
    ```jsx
    client.getHuddleElement();
    ```
</Step>
<Step title="getLiveStateSyncElement()">
    Retrieves the live state sync element, possibly used for managing live document state synchronization.
    ```jsx
    client.getLiveStateSyncElement();
    ```
</Step>
<Step title="disableFeatures()">
    Disables specific features or functionality within the application or service.
    ```jsx
    client.disableFeatures();
    ```
</Step>
<Step title="setUserRoles()">
    Sets user roles and permissions, determining what actions users are allowed to perform.
    ```jsx
    client.setUserRoles();
    ```
</Step>
<Step title="setDarkMode()">
    Enables or disables dark mode, changing the application's or service's visual theme.
    ```jsx
    client.setDarkMode();
    ```
</Step>
<Step title="inviteUsers()">
    Invites users to join the application or specific activities within the service.
    ```jsx
    client.inviteUsers();
    ```
</Step>
<Step title="isUserAllowed$()">
    Checks whether a user is allowed to perform a specific action or access certain features.
    ```jsx
    client.isUserAllowed$();
    ```
</Step>
<Step title="getUserRole$()">
    Retrieves the user's role, indicating their level of access and permissions.
    ```jsx
    client.getUserRole$();
    ```
</Step>
<Step title="getFeaturesAccessLevelIam$()">
    Obtains the user's access level for specific features through the Identity and Access Management system.
    ```jsx
    client.getFeaturesAccessLevelIam$();
    ```
</Step>
<Step title="isPlanExpired$()">
    Checks whether the user's plan or subscription has expired.
    ```jsx
    client.isPlanExpired$();
    ```
</Step>
<Step title="removeSnippylyContent()">
    Removes content related to Snippyly from the application or service.
    ```jsx
    client.removeSnippylyContent();
    ```
</Step>
<Step title="removeVeltContent()">
    Removes content related to Velt from the application or service.
    ```jsx
    client.removeVeltContent();
    ```
</Step>
<Step title="setLanguage()">
    Sets the user's language preferences, determining the language used in the application or service.
    ```jsx
    client.setLanguage();
    ```
</Step>
<Step title="setTranslations()">
    Manages translation settings, configuring how content is translated from one language to another.
    ```jsx
    client.setTranslations();
    ```
</Step>
<Step title="enableAutoTranslation()">
    Enables automatic translation services, allowing content to be automatically translated when necessary.
    ```jsx
    client.enableAutoTranslation();
    ```
</Step>
<Step title="disableAutoTranslation()">
    Disables automatic translation services, preventing content from being automatically translated.
    ```jsx
    client.disableAutoTranslation();
    ```
</Step>
</Steps>