{/* <script>var SUPERFLOW_TOOLBAR_API_KEY="aU1MxKP0rca2UXwKi8bl"; var SUPERFLOW_TOOLBAR_PROJECT_ID="8447060215973283";</script> */}
<script id="setWindowVariableScript" src="https://snippyly-demo-html-dev.web.app/set-window-variable.js?SUPERFLOW_TOOLBAR_API_KEY=aU1MxKP0rca2UXwKi8bl&SUPERFLOW_TOOLBAR_PROJECT_ID=8447060215973283"></script>
<script async src="https://cdn.jsdelivr.net/npm/@usesuperflow/toolbar/superflow.min.js"></script>


<Steps>
<Step title="getAllCommentAnnotations()">
    Retrieves all comment annotations from the system.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.getAllCommentAnnotations();
    ```
</Step>
<Step title="deleteComment()">
    Deletes a comment from the system.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.deleteComment();
    ```
</Step>
<Step title="enableTextComments()">
    Enables text comments for the current user.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableTextComments();
    ```
</Step>
<Step title="disableTextComments()">
    Disables text comments for the current user.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableTextComments();
    ```
</Step>
<Step title="allowedElementIds()">
    Retrieves a list of allowed element IDs.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.allowedElementIds();
    ```
</Step>
<Step title="disableFloatingComments()">
    Disables floating comments in the interface.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableFloatingComments();
    ```
</Step>
<Step title="enableFloatingComments()">
    Enables floating comments in the interface.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableFloatingComments();
    ```
</Step>
<Step title="disableFloatingCommentDialog()">
    Disables the floating comment dialog.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableFloatingCommentDialog();
    ```
</Step>
<Step title="enableFloatingCommentDialog()">
    Enables the floating comment dialog.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableFloatingCommentDialog();
    ```
</Step>
<Step title="attachComment()">
    Attaches a comment to a specific element.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.attachComment();
    ```
</Step>
<Step title="openCommentSidebar()">
    Opens the comment sidebar in the user interface.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.openCommentSidebar();
    ```
</Step>
<Step title="closeCommentSidebar()">
    Closes the comment sidebar in the user interface.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.closeCommentSidebar();
    ```
</Step>
<Step title="toggleCommentSidebar()">
    Toggles the visibility of the comment sidebar.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.toggleCommentSidebar();
    ```
</Step>
<Step title="enableModeratorMode()">
    Enables moderator mode for managing comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableModeratorMode();
    ```
</Step>
<Step title="disableModeratorMode()">
    Disables moderator mode for managing comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableModeratorMode();
    ```
</Step>
<Step title="enableStreamMode()">
    Enables stream mode for viewing comments in real-time.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableStreamMode();
    ```
</Step>
<Step title="disableStreamMode()">
    Disables stream mode for viewing comments in real-time.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableStreamMode();
    ```
</Step>
<Step title="enableSignInButton()">
    Enables the sign-in button in the interface.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableSignInButton();
    ```
</Step>
<Step title="disableSignInButton()">
    Disables the sign-in button in the interface.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableSignInButton();
    ```
</Step>
<Step title="enableUpgradeButton()">
    Enables the upgrade button for premium features.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableUpgradeButton();
    ```
</Step>
<Step title="disableUpgradeButton()">
    Disables the upgrade button for premium features.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableUpgradeButton();
    ```
</Step>
<Step title="addCommentModeChange()">
    Adds a comment mode change event.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.addCommentModeChange();
    ```
</Step>
<Step title="onCommentModeChange()">
    Handles a comment mode change event.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.onCommentModeChange();
    ```
</Step>
<Step title="enableAttachment()">
    Enables attachments for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableAttachment();
    ```
</Step>
<Step title="disableAttachment()">
    Disables attachments for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableAttachment();
    ```
</Step>
<Step title="enableAttachments()">
    Enables the display of attachments in comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableAttachments();
    ```
</Step>
<Step title="disableAttachments()">
    Disables the display of attachments in comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableAttachments();
    ```
</Step>
<Step title="isUserGlobalContact()">
    Checks if the user is a global contact in the system.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.isUserGlobalContact();
    ```
</Step>
<Step title="showDeviceInfo()">
    Displays device information in the user interface.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.showDeviceInfo();
    ```
</Step>
<Step title="enableDeviceInfo()">
    Enables the display of device information.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableDeviceInfo();
    ```
</Step>
<Step title="disableDeviceInfo()">
    Disables the display of device information.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableDeviceInfo();
    ```
</Step>
<Step title="enableCommentMode()">
    Enables a specific comment mode in the system.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableCommentMode();
    ```
</Step>
<Step title="disableCommentMode()">
    Disables a specific comment mode in the system.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableCommentMode();
    ```
</Step>
<Step title="enablePersistentCommentMode()">
    Enables persistent comment mode.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enablePersistentCommentMode();
    ```
</Step>
<Step title="disablePersistentCommentMode()">
    Disables persistent comment mode.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disablePersistentCommentMode();
    ```
</Step>
<Step title="showCommentIndex()">
    Displays the comment index in the user interface.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.showCommentIndex();
    ```
</Step>
<Step title="enableCommentIndex()">
    Enables the comment index feature.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableCommentIndex();
    ```
</Step>
<Step title="disableCommentIndex()">
    Disables the comment index feature.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableCommentIndex();
    ```
</Step>
<Step title="enablePopoverMode()">
    Enables popover mode for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enablePopoverMode();
    ```
</Step>
<Step title="disablePopoverMode()">
    Disables popover mode for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disablePopoverMode();
    ```
</Step>
<Step title="enableDialogOnHover()">
    Enables comment dialog on hover.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableDialogOnHover();
    ```
</Step>
<Step title="enablePopoverTriangleComponent()">
    Enables the popover triangle component for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enablePopoverTriangleComponent();
    ```
</Step>
<Step title="disablePopoverTriangleComponent()">
    Disables the popover triangle component for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disablePopoverTriangleComponent();
    ```
</Step>
<Step title="disableDialogOnHover()">
    Disables comment dialog on hover.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableDialogOnHover();
    ```
</Step>
<Step title="enableDialogOnTargetElementClick()">
    Enables comment dialog on target element click.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableDialogOnTargetElementClick();
    ```
</Step>
<Step title="disableDialogOnTargetElementClick()">
    Disables comment dialog on target element click.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableDialogOnTargetElementClick();
    ```
</Step>
<Step title="enablePriority()">
    Enables priority settings for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enablePriority();
    ```
</Step>
<Step title="disablePriority()">
    Disables priority settings for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disablePriority();
    ```
</Step>
<Step title="enableStatus()">
    Enables status settings for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableStatus();
    ```
</Step>
<Step title="disableStatus()">
    Disables status settings for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableStatus();
    ```
</Step>
<Step title="enableResolve()">
    Enables the resolve feature for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableResolve();
    ```
</Step>
<Step title="disableResolve()">
    Disables the resolve feature for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableResolve();
    ```
</Step>
<Step title="enableResolveButton()">
    Enables the resolve button for individual comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableResolveButton();
    ```
</Step>
<Step title="disableResolveButton()">
    Disables the resolve button for individual comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableResolveButton();
    ```
</Step>
<Step title="enableGhostCommentsInDOM()">
    Enables the display of ghost comments in the Document Object Model (DOM).
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableGhostCommentsInDOM();
    ```
</Step>
<Step title="disableGhostCommentsInDOM()">
    Disables the display of ghost comments in the Document Object Model (DOM).
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableGhostCommentsInDOM();
    ```
</Step>
<Step title="enableGhostComments()">
    Enables ghost comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableGhostComments();
    ```
</Step>
<Step title="disableGhostComments()">
    Disables ghost comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableGhostComments();
    ```
</Step>
<Step title="enableGhostCommentsMessage()">
    Enables the display of a message for ghost comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableGhostCommentsMessage();
    ```
</Step>
<Step title="disableGhostCommentsMessage()">
    Disables the display of a message for ghost comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableGhostCommentsMessage();
    ```
</Step>
<Step title="enableGhostCommentsIndicator()">
    Enables the display of an indicator for ghost comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableGhostCommentsIndicator();
    ```
</Step>
<Step title="disableGhostCommentsIndicator()">
    Disables the display of an indicator for ghost comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableGhostCommentsIndicator();
    ```
</Step>
<Step title="setCustomStatuses()">
    Sets custom statuses for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.setCustomStatuses();
    ```
</Step>
<Step title="setCustomStatus()">
    Sets a custom status for a comment.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.setCustomStatus();
    ```
</Step>
<Step title="setCustomPriorities()">
    Sets custom priorities for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.setCustomPriorities();
    ```
</Step>
<Step title="setCustomPriority()">
    Sets a custom priority for a comment.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.setCustomPriority();
    ```
</Step>
<Step title="setCustomCategories()">
    Sets custom categories for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.setCustomCategories();
    ```
</Step>
<Step title="setCustomCategory()">
    Sets a custom category for a comment.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.setCustomCategory();
    ```
</Step>
<Step title="enableInboxMode()">
    Enables inbox mode for managing comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableInboxMode();
    ```
</Step>
<Step title="disableInboxMode()">
    Disables inbox mode for managing comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableInboxMode();
    ```
</Step>
<Step title="enableAutoCategorize()">
    Enables automatic categorization of comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableAutoCategorize();
    ```
</Step>
<Step title="disableAutoCategorize()">
    Disables automatic categorization of comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableAutoCategorize();
    ```
</Step>
<Step title="enableDarkMode()">
    Enables dark mode for the user interface.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableDarkMode();
    ```
</Step>
<Step title="disableDarkMode()">
    Disables dark mode for the user interface.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableDarkMode();
    ```
</Step>
<Step title="setContextProvider()">
    Sets a context provider for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.setContextProvider();
    ```
</Step>
<Step title="enableSuggestionMode()">
    Enables suggestion mode for comment recommendations.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableSuggestionMode();
    ```
</Step>
<Step title="disableSuggestionMode()">
    Disables suggestion mode for comment recommendations.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableSuggestionMode();
    ```
</Step>
<Step title="enableMobileMode()">
    Enables mobile mode for a mobile-friendly interface.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableMobileMode();
    ```
</Step>
<Step title="disableMobileMode()">
    Disables mobile mode for a mobile-friendly interface.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableMobileMode();
    ```
</Step>
<Step title="enableInlineCommentMode()">
    Enables inline comment mode for comments within content.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableInlineCommentMode();
    ```
</Step>
<Step title="disableInlineCommentMode()">
    Disables inline comment mode for comments within content.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableInlineCommentMode();
    ```
</Step>
<Step title="enableMinimap()">
    Enables the minimap feature for navigating comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableMinimap();
    ```
</Step>
<Step title="disableMinimap()">
    Disables the minimap feature for navigating comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableMinimap();
    ```
</Step>
<Step title="showCommentsOnDom()">
    Displays comments in the Document Object Model (DOM).
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.showCommentsOnDom();
    ```
</Step>
<Step title="hideCommentsOnDom()">
    Hides comments in the Document Object Model (DOM).
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.hideCommentsOnDom();
    ```
</Step>
<Step title="enableCommentTool()">
    Enables the comment tool for creating and managing comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.enableCommentTool();
    ```
</Step>
<Step title="disableCommentTool()">
    Disables the comment tool for creating and managing comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.disableCommentTool();
    ```
</Step>
<Step title="setTotalMediaLength()">
    Sets the total media count for comments.
    ```jsx
    const commentElement = client.getCommentElement();
    commentElement.setTotalMediaLength();
    ```
</Step>

</Steps>